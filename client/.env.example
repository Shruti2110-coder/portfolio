# Base URL of the portfolio API, no trailing slash and no /api suffix.
# Leave BLANK for local dev — the Vite proxy handles it.
# Production example: https://portfolio-api-xxxx.onrender.com
VITE_API_URL=
